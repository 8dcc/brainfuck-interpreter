### Command list
List of available commands:

Cmd         | Description
------------|-----------------------
`help`      | Displays a list of commands
`quit`      | Exit the program
`ref`       | Calls `refresh()`
